{
  "name": "flareon",
  "private": true,
  "version": "1.0.0",
  "description": "Daily standup reporter Slack bot for @itsmagic",
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild --bundle --outdir=dist ./src/index.ts",
    "format": "prettier --write  '*.{json,js}' 'src/**/*.{js,ts}' 'test/**/*.{js,ts}'",
    "lint": "eslint --max-warnings=0 src && prettier --check '*.{json,js}' 'src/**/*.{js,ts}' 'test/**/*.{js,ts}'",
    "test": "jest --config jestconfig.json --verbose",
    "dev": "miniflare --watch --debug --port 3000"
  },
  "author": {
    "name": "Cristopher Namchee",
    "email": "cristophernamchee12@gmail.com"
  },
  "license": "MIT",
  "eslintConfig": {
    "root": true,
    "extends": [
      "typescript",
      "prettier"
    ]
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^3.0.0",
    "@typescript-eslint/eslint-plugin": "^5.8.1",
    "@typescript-eslint/parser": "^5.8.1",
    "esbuild": "^0.14.8",
    "eslint": "^8.5.0",
    "eslint-config-xo": "^0.39.0",
    "eslint-plugin-jsdoc": "^37.4.2",
    "typescript": "^4.3.2",
    "vite": "^2.7.9",
    "vitest": "^0.0.118"
  }
}
